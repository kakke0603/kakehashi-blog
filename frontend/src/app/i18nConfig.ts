export const i18nConfig = {
  locales: ["en", "ja"],
  defaultLocale: "ja",
};

import { InitOptions } from "i18next";

export const i18nextInitOptions: InitOptions = {
  lng: "ja",
  fallbackLng: "ja",
};
